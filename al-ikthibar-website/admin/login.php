<?php require '../includes/functions.php';
if(!q("SELECT COUNT(*) FROM users")->fetchColumn()) q("INSERT INTO users(name,email,password_hash,role) VALUES('Admin','admin@alikthibar.com',?, 'admin')",[password_hash('admin123',PASSWORD_DEFAULT)]);
$m='';if($_SERVER['REQUEST_METHOD']=='POST'&&csrf_ok()){$u=q("SELECT * FROM users WHERE email=?",[$_POST['email']])->fetch();
 if($u&&password_verify($_POST['password'],$u['password_hash'])){session_regenerate_id(true);$_SESSION['uid']=$u['id'];header('Location: index.php');exit;}$m='Wrong email or password.';} ?>
<!doctype html><html data-bs-theme="dark"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Admin login</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link href="../assets/style.css" rel="stylesheet"></head><body>
<div class="wrap narrow" style="max-width:420px;padding-top:12vh"><form method="post" class="glass form"><h3>Admin login</h3><?php if($m) echo '<div class="alert alert-danger">'.e($m).'</div>'; ?><?=csrf_field()?><label>Email</label><input name="email" type="email" required><label>Password</label><input name="password" type="password" required><button class="btn-g w-100 mt-3">Log in</button><p class="mt-3 small">First login: admin@alikthibar.com / admin123 — change it right away.</p></form></div></body></html>
